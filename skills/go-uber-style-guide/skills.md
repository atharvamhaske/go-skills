# go-uber-style-guide

Compatibility entrypoint for skill-based agents.

Use `SKILL.md` as the canonical instruction file.
If an agent expects lowercase skill docs, load this file first and then `SKILL.md`.

